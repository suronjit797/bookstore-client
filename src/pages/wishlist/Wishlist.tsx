// import React from 'react';

import Layout from "../../layouts/Layout";

const Wishlist = () => {
    return (
        <Layout>
            wishListHandler
        </Layout>
    );
};

export default Wishlist;